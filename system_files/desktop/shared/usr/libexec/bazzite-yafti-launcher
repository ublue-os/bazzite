#!/bin/bash

# Start yafti-go in background
~/.local/bin/yafti-go &
YAFTI_PID=$!

# Launch Chromium in app mode with custom window size
flatpak run org.chromium.Chromium --app=http://localhost:3169 --window-size=1280,800

# Kill yafti-go when Chromium window is closed
kill $YAFTI_PID
